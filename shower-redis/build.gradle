group 'com.idea.shower.redis'
dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-data-redis'
//    compile project(':shower-redis')
}

bootJar {
    enabled = false
}